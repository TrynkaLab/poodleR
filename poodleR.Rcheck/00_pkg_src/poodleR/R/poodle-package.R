#' poodleR: pooled donor deconvolution helpers
#'
#' `poodleR` provides a small workflow for pooled sequencing deconvolution:
#' convert donor genotypes in a VCF to allele dosages, derive pooled allele
#' frequencies from bam-readcount output, and estimate donor weights under
#' simplex constraints.
#'
#' The package ships with compressed example files in
#' `system.file("extdata", package = "poodleR")` and small `.rda` objects for
#' reproducible examples and tests.
#'
#' @keywords internal
"_PACKAGE"
